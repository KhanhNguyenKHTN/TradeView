{
  "name": "tradeview",
  "version": "1.0.0",
  "description": "common package json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:fe": "cd frontend && npm run build",
    "build:be": "cd backend && npm run build",
    "build": "npm run build:fe && npm run build:be",
    "deploy:fe": "deploy.fe.ps1",
    "deploy:be": "deploy.be.ps1",
    "deploy": "deploy.ps1"
  },
  "author": "",
  "license": "ISC"
}
